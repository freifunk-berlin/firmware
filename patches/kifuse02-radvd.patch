Index: ipv6/radvd/files/radvd.hotplug
===================================================================
--- ipv6/radvd/files/radvd.hotplug	(revision 0)
+++ ipv6/radvd/files/radvd.hotplug	(revision 0)
@@ -0,0 +1,3 @@
+#!/bin/sh
+killall -HUP radvd
+
Index: ipv6/radvd/Makefile
===================================================================
--- ipv6/radvd/Makefile	(revision 18526)
+++ ipv6/radvd/Makefile	(working copy)
@@ -10,7 +10,7 @@
 
 PKG_NAME:=radvd
 PKG_VERSION:=1.2
-PKG_RELEASE:=1.1
+PKG_RELEASE:=1.2
 
 PKG_BUILD_DIR:=$(BUILD_DIR)/$(PKG_NAME)-$(PKG_VERSION)
 PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.gz
@@ -59,6 +59,8 @@
 	$(INSTALL_DIR) $(1)/usr/sbin
 	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/usr/sbin/radvd $(1)/usr/sbin/
 	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/usr/sbin/radvdump $(1)/usr/sbin/
+	$(INSTALL_DIR) $(1)/etc/hotplug.d/iface
+	$(INSTALL_DATA) ./files/radvd.hotplug $(1)/etc/hotplug.d/iface/40-radvd
 endef
 
 $(eval $(call BuildPackage,radvd))
