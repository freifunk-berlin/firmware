Index: target/linux/x86/image/Config.in
===================================================================
--- target/linux/x86/image/Config.in	(Revision 31059)
+++ target/linux/x86/image/Config.in	(Arbeitskopie)
@@ -43,6 +43,12 @@
 	depends TARGET_ROOTFS_EXT2FS
 	select PACKAGE_kmod-e1000
 
+config X86_QCOW2_IMAGES
+	bool "Build kvm image files (QCOW2)"
+	depends X86_GRUB_IMAGES
+	depends TARGET_x86_generic
+	depends TARGET_ROOTFS_SQUASHFS
+	select PACKAGE_kmod-e1000
 
 config OLPC_BOOTSCRIPT_IMAGES
 	bool "Build images with bootscript"
Index: target/linux/x86/image/Makefile
===================================================================
--- target/linux/x86/image/Makefile	(Revision 31059)
+++ target/linux/x86/image/Makefile	(Arbeitskopie)
@@ -175,6 +175,20 @@
   endef
 endif
 
+ifneq ($(CONFIG_X86_QCOW2_IMAGES),)
+  define Image/Build/qcow2
+    # left here because the image builder doesnt need these
+    ifeq ($(1),squashfs)
+		rm $(BIN_DIR)/$(IMG_PREFIX)-combined-$(1).qcow2 || true
+		qemu-img convert -f raw -O qcow2 \
+			$(BIN_DIR)/$(IMG_PREFIX)-combined-$(1).img \
+			$(BIN_DIR)/$(IMG_PREFIX)-combined-$(1).qcow2
+		qemu-img resize \
+			$(BIN_DIR)/$(IMG_PREFIX)-combined-$(1).qcow2 +23M
+    endif
+  endef
+endif
+
 define Image/BuildKernel
 	$(CP) $(KDIR)/bzImage $(BIN_DIR)/$(IMG_PREFIX)-vmlinuz
 endef
@@ -192,6 +206,7 @@
 	$(call Image/Build/grub,$(1))
 	$(call Image/Build/vdi,$(1))
 	$(call Image/Build/vmdk,$(1))
+	$(call Image/Build/qcow2,$(1))
   endif
 	$(CP) $(KDIR)/root.$(1) $(BIN_DIR)/$(IMG_PREFIX)-rootfs-$(1).img
 	$(CP) $(KDIR)/bzImage $(BIN_DIR)/$(IMG_PREFIX)-vmlinuz
Index: tools/qemu/Makefile
===================================================================
--- tools/qemu/Makefile	(Revision 31059)
+++ tools/qemu/Makefile	(Arbeitskopie)
@@ -7,12 +7,12 @@
 include $(TOPDIR)/rules.mk
 
 PKG_NAME:=qemu
-PKG_VERSION:=0.12.5
-PKG_RELEASE:=2
+PKG_VERSION:=0.14.1
+PKG_RELEASE:=1
 
 PKG_SOURCE_URL:=http://download.savannah.gnu.org/releases/qemu/
 PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.gz
-PKG_MD5SUM:=1d02ee0a04dfae2894340273372c1de4
+PKG_MD5SUM:=b6c713a8db638e173af53a62d5178640
 
 include $(INCLUDE_DIR)/host-build.mk
 
