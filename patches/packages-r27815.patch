Index: branches/backfire/package/swconfig/src/swlib.c
===================================================================
--- branches/backfire/package/swconfig/src/swlib.c	(revision 27626)
+++ branches/backfire/package/swconfig/src/swlib.c	(revision 27815)
@@ -581,4 +581,5 @@
 	struct switch_dev *dev;
 	const char *name;
+	const char *alias;
 
 	if (nla_parse(tb, SWITCH_ATTR_MAX, genlmsg_attrdata(gnlh, 0), genlmsg_attrlen(gnlh, 0), NULL) < 0)
@@ -589,5 +590,7 @@
 
 	name = nla_get_string(tb[SWITCH_ATTR_DEV_NAME]);
-	if (sa->name && (strcmp(name, sa->name) != 0))
+	alias = nla_get_string(tb[SWITCH_ATTR_ALIAS]);
+
+	if (sa->name && (strcmp(name, sa->name) != 0) && (strcmp(alias, sa->name) != 0))
 		goto done;
 
@@ -596,5 +599,6 @@
 		goto done;
 
-	dev->dev_name = strdup(name);
+	strncpy(dev->dev_name, name, IFNAMSIZ - 1);
+	dev->alias = strdup(alias);
 	if (tb[SWITCH_ATTR_ID])
 		dev->id = nla_get_u32(tb[SWITCH_ATTR_ID]);
Index: branches/backfire/package/swconfig/src/swlib.h
===================================================================
--- branches/backfire/package/swconfig/src/swlib.h	(revision 20948)
+++ branches/backfire/package/swconfig/src/swlib.h	(revision 27815)
@@ -115,6 +115,7 @@
 struct switch_dev {
 	int id;
+	char dev_name[IFNAMSIZ];
 	const char *name;
-	const char *dev_name;
+	const char *alias;
 	int ports;
 	int vlans;
Index: branches/backfire/package/swconfig/src/cli.c
===================================================================
--- branches/backfire/package/swconfig/src/cli.c	(revision 20948)
+++ branches/backfire/package/swconfig/src/cli.c	(revision 27815)
@@ -75,5 +75,5 @@
 list_attributes(struct switch_dev *dev)
 {
-	printf("Switch %d: %s(%s), ports: %d (cpu @ %d), vlans: %d\n", dev->id, dev->dev_name, dev->name, dev->ports, dev->cpu_port, dev->vlans);
+	printf("%s: %s(%s), ports: %d (cpu @ %d), vlans: %d\n", dev->dev_name, dev->alias, dev->name, dev->ports, dev->cpu_port, dev->vlans);
 	printf("     --switch\n");
 	print_attrs(dev->ops);
Index: branches/backfire/package/swconfig/Makefile
===================================================================
--- branches/backfire/package/swconfig/Makefile	(revision 24992)
+++ branches/backfire/package/swconfig/Makefile	(revision 27815)
@@ -9,5 +9,5 @@
 
 PKG_NAME:=swconfig
-PKG_RELEASE:=7
+PKG_RELEASE:=8
 
 include $(INCLUDE_DIR)/package.mk
