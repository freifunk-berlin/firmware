--- a/feeds/luci/modules/freifunk/luasrc/view/freifunk/index.htm
+++ b/feeds/luci/modules/freifunk/luasrc/view/freifunk/index.htm
@@ -12,13 +12,38 @@ You may obtain a copy of the License at
 $Id$
 
 -%>
-<%+header%>
-<% 
+<%
 local uci = require "luci.model.uci".cursor()
+local sys = require "luci.sys"
 local tpl = require "luci.template"
 local fs = require "luci.fs"
 local ff = {}
 local ff = uci:get_all("freifunk")
+local http = require "luci.http"
+local disp = require "luci.dispatcher"
+local ipkg = require "luci.model.ipkg"
+
+-- only redirect if assistent is installed and no root password is set
+local webAppRoot = http.getenv("PATH_INFO") == nil
+local notRunBefore = not uci.get("ffwizard","settings","runbefore")
+--local rootPwNotSet = luci.sys.user.getuser("root") and not luci.sys.user.getpasswd("root")
+--ipkg.installed() needs root that's why we cannot use it here
+--local wizardInstalled = ipkg.installed("luci-app-ffwizard-berlin")
+local wizardInstalled = fs.isfile("/usr/lib/lua/luci/controller/assistent/assistent.lua")
+--sys.exec("logger -t ffwizard -p 5 '"..tostring(webAppRoot).."'")
+--sys.exec("logger -t ffwizard -p 5 '"..tostring(notRunBefore).."'")
+--sys.exec("logger -t ffwizard -p 5 '"..tostring(rootPwNotSet).."'")
+--sys.exec("logger -t ffwizard -p 5 '"..tostring(wizardInstalled).."'")
+if (webAppRoot and notRunBefore and wizardInstalled) then
+	local url = luci.dispatcher.build_url("admin/freifunk/assistent")
+	url = url .. "?luci_username=root&luci_password="
+	http.redirect(url)
+end
+%>
+
+<%+header%>
+
+<%
 
 if not ff or not ff.community.name then
 	community = "Freifunk"
